import { MaxDateRangeDirective } from './max-date-range.directive';

describe('MaxDateRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxDateRangeDirective();
    expect(directive).toBeTruthy();
  });
});
